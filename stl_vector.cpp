#include<iostream>
#include<vector>
using namespace std;

int main()
{
    vector<int > v;
    v.emplace_back(5);
    v.pop_back();

    cout<<"Enter elements: ";

    for(int i=0; i<10;i++)
    {
        v.emplace_back(i);
    }

    for(auto i:a)
    {
        cout<<
    }
}